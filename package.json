{
  "name": "playwright-api-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight Playwright API automation framework using TypeScript and JSONPlaceholder",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^26.1.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
